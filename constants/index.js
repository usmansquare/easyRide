import {COLORS, SIZES, FONTS, lightTheme, darkTheme, selectedTheme } from './theme'
import images from './images'
import icons from './icons'

export {
    COLORS,
    FONTS,
    SIZES,
    images,
    icons,
    lightTheme,
    darkTheme,
    selectedTheme
}